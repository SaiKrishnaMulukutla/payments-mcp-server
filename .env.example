PAYMENTS_BACKEND=demo                 # demo | http

PAYMENTS_BASE_URL=http://localhost:8080
# PAYMENTS_TOKEN=                     # bearer for the real backend; NEVER exposed to the model
PAYMENTS_MERCHANT_ID=mcp-agent

# Required whenever the approval service is enabled; there is no in-memory fallback.
# PAYMENTS_APPROVAL_DATABASE_URL=postgresql://payments_mcp:change-me@localhost:5432/payments_mcp
PAYMENTS_APPROVAL_TTL_SECONDS=900

ANTHROPIC_API_KEY=<api_key>
